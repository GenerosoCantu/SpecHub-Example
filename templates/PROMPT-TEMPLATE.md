<!--
  IMPLEMENTATION PROMPT TEMPLATE
  ==============================
  Copy to `Prompts/PROMPT-{service}-{feature}.md` — one per affected service.
  Normally generated by the `generate-prompts` skill (WORKFLOW.md Step 3), not
  written by hand; this template documents the required shape.

  A prompt must be stateless and self-contained: the implementing session runs
  in a different workspace and cannot follow a reference back to a spec file.
  If a brand-new session in the target repo would need to ask a question or
  read an unnamed file, the prompt is incomplete — fix it before implementing.

  Delete this comment block before committing.
-->

# Task: {imperative description of the implementation}

> **Target repo:** {repo path/name}
> **Branch:** feature/{kebab-name}
> **Prerequisites:** {PROMPT-file(s) that must be Verified first, or "none"}
> **Status:** Generated   <!-- Generated → Applied → Verified -->
> **Recommended model:** {tier} — {one-line reason, carried from the feature file}

## Context

{2–5 sentences: what this adds and why, including any cross-service background the implementer needs (e.g. snapshot paths or endpoints consumed).}

## Relevant Existing Files to Study Before Implementing

```text
src/{...}   ← Reference: {pattern it demonstrates}
src/{...}   ← UPDATE: {what changes}
```

## New Files to Create

```text
src/{...}
```

## Schema Contract

{Field | Type | Required | Default | Notes table — verbatim from the spec.}

## Endpoints

{Method | Path | Guard | Request/Response — verbatim from the spec.}

## Naming Rules

{DTO names, Redux slice/actions, route strings — verbatim from the spec.}

## {Backend only} Validation, Errors, Tests

{DTO constraints, guard behavior, error/status semantics, required test updates.}

## {Frontend only} Views, State, UX States, Acceptance Criteria

{Routes/views/components, state-management updates, loading/empty/error/permission states, acceptance criteria.}
