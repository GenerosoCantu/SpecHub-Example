# Plaza Spec Hub — Copilot Instructions

This file is a **pointer**. The single source of truth for workspace
instructions is [`CLAUDE.md`](../CLAUDE.md) at the repo root — read and follow
it in full.

(Looking for the *per-service-repo* routing file this example used to show
here? It moved to `repo-instructions/main-api.md`, where the hub keeps the
canonical copy of every service repo's instruction file — see
`repo-instructions/README.md`.)
